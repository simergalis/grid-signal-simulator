Q6 is clean — `RunContext.sim_time` is the anchor, `tick_seq` tracks it, and the persisted interval-end value is write-only. The one thing to preserve: your note that a future restart path must set `sim_time = tick_seq × dt` is now a documented requirement nothing enforces. Worth a test when a restart path actually exists.

**The Step 8 plan has two defects in the TC-33 scenarios**, which is the part I'd most want right since it's the demo's least intuitive result.

**A — the two ΔP values don't match.** `P_compute = nodes × kW × PUE_base / 1000`, so 600 nodes × 10.2 kW × 1.03 = **6.3036 MW**. Your 6.12 MW is the *raw IT* load before `PUE_base`. Sizing solar at 6.12 MW gives a renewable step of 6.12 against a compute step of 6.3036 — a 2.9% mismatch, so the equivalence assertion either fails or needs a tolerance loose enough to prove nothing.

**B — the renewable step never invokes staging.** `stage_for_predicted_step` is called from `apply_workload_signal`, on workload events only. An irradiance change moves `p_dispatch_required` on the next tick and the arbitrator reacts through `tick()` — no reserve check, no alert. So the two scenarios aren't comparable: one stages and alerts, the other quietly ramps.

TC-33's "identical staging response" means the same arithmetic with `Δt_lead = 0`: `gap = ΔP/r_asset − 0`. The reserve check should run and the alert should fire. You need an irradiance step to call the same staging path with zero lead — that's §7.1.1's *"a renewable shortfall carries no advance signal"* made executable rather than asserted.

**Three smaller ones:**

**C — the C-rate guard shouldn't return 400.** PROTO-9 is explicitly *chosen, no measured basis*. Rejecting a scenario on an unmeasured bound stops an operator modelling a real system that happens to sit outside our guess. Warn in both places; return the warning as a response field, not an error.

**D — you're building an in-memory store next to an unused table.** Step 2 created the `Scenario` entity per Functional Spec §8.1. Use it rather than adding a second scenario store to reconcile in Step 9.

**E — `grid_forming` "at most one per scenario" is tagged D12.** D12 is the C-rate guard; the single-anchor constraint is §7.1.2. And it's documented, not validated — `demo-fleet` is the first scenario where two units could both claim it.

**Minor:** `node_count [1..8192]` contradicts demo-tc33-compute's "0 nodes at t=0"; and `irradiance_steps` has two entries at t=30.0, so the piecewise convention needs stating (I'd take "value applies from t onward," making the duplicate unnecessary).

Everything else is approved — the drawer-not-modal choice, removing `scenario_preset` rather than keeping two paths, the live C-rate indicator mirroring the backend, and `demo-fleet`'s 18/5 MW split exercising both the proportional allocation and D13's `min()`.

Fix A through E, then build. Same reporting set as before, plus vitest.