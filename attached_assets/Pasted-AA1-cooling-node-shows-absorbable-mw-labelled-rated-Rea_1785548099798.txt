AA1 — cooling node shows absorbable_mw labelled "rated". Reads "3.99 MW" against "0.60 MW
  rated" — 6.6x its own rating. 4.59 − 3.99 = 0.60, so the sub-line is bound to the wrong
  field. Bind to rated_cooling_mw. If you want headroom on the node, label it "0.60 MW
  headroom" as a separate value, never as "rated".

AA2 — BRIDGE reads "0 s" when there is no shortfall. 0 s is the CANNOT-BRIDGE signal (D11:
  a battery below the required power bridges for zero seconds). No shortfall is the opposite
  condition. Use the bridging_basis field that already exists: basis "no_load" → "full
  reserve"; basis "current_demand" or "predicted_peak" → the duration. The panel and the
  verdict must never contradict — that was F2.

AA3 — DISPATCHABLE changes meaning between states. At rest: 48.0 MW, "turbine + BESS + solar"
  (25 + 18 + 4.99 ✓). Running: 25.0 MW, "turbine + solar" — which is neither 25 + 4.99 = 29.99
  nor the at-rest figure. The BESS silently left the sum and the arithmetic does not match the
  label. Same definition in both states.

AA4 — LEAD TIME hero reads "—" while the callout beside it reads "0 s". Same quantity, two
  displays, one blank. Bind both to dt_lead_next_s.

AA5 — Network Fabric tile reads "Network telemetry not instrumented in this tick" during a
  run, but W1 wired NetworkTelemetryIngestor into _drive() and demo-20mw injects two synthetic
  records per tick. At rest the same tile read "2 switches reporting — one at NTP only".
  Either the tile is reading the wrong field or the ingest is not running. Which?

AA6 — websockets install path will not survive a rebuild.
  `pip3 install --target=.pythonlibs/...` is a working-tree fix, exactly like the node_modules
  fix earlier. Add websockets to the declared dependencies so a fresh Replit build gets it.
  Then confirm: does a clean install produce a working WebSocket, or does the frontend go
  silent again?

AA7 — load test scale, asked four times now. 57.8 s is either 1x (a 3x regression, since 1x
  has run 16-21 s and has NEVER exceeded 30 s) or 4x (long-standing). "Zero Python changed" is
  a good argument but does not identify the scale. Report 1x wall clock and 2x wall clock
  separately, labelled.