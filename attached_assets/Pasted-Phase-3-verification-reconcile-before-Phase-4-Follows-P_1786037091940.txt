Phase 3 verification — reconcile before Phase 4

Follows: Phase 3 of the dynamic-configuration refactor (Guard E Tier 1 cleared). Blocks: Phases 4–7. Scope: verification and correction. Four items. No new phases, no Tier-2 work.

Guard D1, D2 and E Tier 1 all pass — that part is done. Four things need settling before Phases 4–7 are gated against this state, because the baseline moved and the phase boundary was crossed.

Item 1 — 19 passes disappeared (blocking)

Baseline after triage: 12 failed, 963 passed, 0 errors, 975 collected. Phase 3 reports: 12 failed, 944 passed.

Same failure count, 19 fewer passes. A frontend-only phase cannot move the Python count.

The summary attributes 18 fabric failures to "a pre-existing CWD path issue in fabric_engine.py", but that does not reconcile: 18 additional failures against 963 passes gives neither 12 failed nor 944 passed. Either those failures are counted somewhere the summary does not show, or roughly 19 tests stopped being collected. A collection change presents exactly like this and silently removes coverage.

Report, with raw output:

pytest --collect-only -q | tail -1 — collected count now, against the baseline 975.
Full pytest -q tail: failed / passed / error / skipped / deselected counts.
The complete list of failing test node IDs, not a category summary.
If the collected count dropped: which tests are no longer collected, and what stopped collecting them.
If the fabric failures are real: were they failing at the 12/963/0 baseline? "Pre-existing" is a claim about that run, and that run reported 0 errors. Show the baseline result for the same node IDs.

Do not fix the fabric issue in this session. Establish whether it is new or pre-existing, and restate the true baseline.

Gate: a reconciled figure — collected, passed, failed, errors — with any delta from 975/963/12/0 attributed to a named cause.

Item 2 — Four Class B fields crossed the phase boundary (blocking)

Phase 3 was scoped to Class A: values already on the tick payload. Four of the seven landed are Class B, and did not exist on the payload when the phase began:

tick.bess_units[].rated_mw
tick.bess_units[].usable_mwh
tick.dt_thermal_seconds
tick.alpha_max

Two possibilities, and they need distinguishing before anything else is built on them.

(a) The payload was extended during Phase 3. Then Phase 4's gate was skipped. Report, per field: the types.ts entry, the backend emission site, and — per task #191 — evidence the typed field landed before the backend emitted the key. The field-boundary mismatch this rule exists to prevent has already recurred three times in this codebase.

(b) The fields do not exist and optional chaining is masking it. tick.bess_units?.[0]?.rated_mw returns undefined silently on every tick, and the panel renders "not instrumented" — which looks exactly like a working derivation until someone reads the screen.

Determine which, with evidence from a live tick. For each of the four fields, capture an actual broadcast payload and show the key present with a numeric value. Then confirm the corresponding panel row renders a number, not "not instrumented".

This is the item most likely to have gone wrong quietly. ?. converts a missing field into a plausible-looking empty state rather than an error.

Gate: each of the four fields shown present on a live tick and rendering a value, or reported absent and the panel reverted to its Class A scope.

Item 3 — turbine_units[0] is not a fleet figure

generation.ts now reads tick.turbine_units[0]?.rated_mw and [0]?.r_asset_mw_per_s. That is the first unit's nameplate standing in for a fleet quantity, correct only while every unit is identical. UC-6 explicitly leaves heterogeneous-fleet behaviour unresolved, so this hardcodes an assumption the spec has deliberately left open — a hardcoded constant replaced by a hardcoded assumption.

Use installedMW() and largestUnitMW() from config/siteParameters.ts for fleet quantities. Reserve indexed per-unit reads for genuinely per-unit displays.

The narrative copy has the same problem. generation.ts:51 reads "Five megawatts of nameplate sit unused" — arithmetic against the old 25 MW literal. Derive it, or remove it. A sentence that states a number the panel no longer computes is worse than the literal it replaced.

Gate: no fleet-level figure in generation.ts derived from a single indexed unit; no narrative sentence stating an underived number.

Item 4 — Two things the report did not mention

generation.ts:49 had two literals. (RAMP_MW_S * 45) and the prose "in the 45 s of warning the scheduler gives". The summary shows RAMP_MW_S derived and says nothing about the 45 or the sentence. Line 57 of the same file already uses tick.dt_lead_next_s. Confirm both moved, or move the prose now.

peakSiteLoadMW must be labelled "observed this run". Required by DR-2026-08-06 D-04. It returns the peak observed so far, so it understates early in a run and the N−1 verdict reads optimistic until the site has actually seen its peak. An unlabelled optimistic verdict is worse than the literal it replaced, because nobody knows to distrust it. Show the rendered label.

Gate: both confirmed in rendered output, not in source alone.

Prohibited
Fixing the fabric/CWD issue.
Fixing any of the 3 class C core defects.
Touching test_tc_p0_1/2/3/5 — deferred to the ramp-algorithm work.
Starting Phase 4, 5, 6 or 7.
Any Tier-2 constant work. FRAME_INTERVAL_MS and the layout values are correctly out of scope; SOC_FLOOR_DEFAULT and SOC_CEIL_DEFAULT are the only physics in that list and they wait.
Adding a Guard E exemption, or narrowing its pattern, to keep Tier 1 green.
Substituting a default where a derived value is unavailable. Render "not instrumented".
Modifying gridsignal_logger.py.
Acceptance criteria
 Collected/passed/failed/error counts reported raw; every delta from 975/963/12/0 attributed.
 Fabric failures shown as pre-existing at the baseline, or reported as new.
 Each of the four Class B fields shown present on a live tick with a numeric value.
 For each: types.ts entry confirmed to precede backend emission, or the field reported absent.
 Every panel row fed by those fields shown rendering a number, not "not instrumented".
 generation.ts fleet figures use installedMW() / largestUnitMW(), not turbine_units[0].
 The "five megawatts" sentence derived or removed.
 generation.ts:49 prose and arithmetic both sourced from dt_lead_next_s.
 peakSiteLoadMW renders with an "observed this run" label.
 Guards D1, D2, E Tier 1 still green at the end.
Project content
GridSignal
Created by you
GridSignal-Forecast-Engine-Functional-Spec_v2.5.md

1,683 lines

MD

PDF

PDF

Content
gridsignal_source_review.zip

ZIP

gridsignal_ui_components.zip

ZIP

The gas turbine generators powered up all at once, which is not possible. One turbine shoiuld have started, increasing its oputput, and rthwen another turbine should start up Fix the logic underlying the ramp up 12 minutes ago Refining turbine startup logic Refining turbine startup logi

PASTED

The test_kube_oscillation docstring literally says "With the current hardcode this test FAILS — that is the point" — it's an intentional red test, pre-existing. I need to fix the 3 real regressions: test_d10, test_tc84f, and test_tc_gt2. Let me read their full failures and test code in parallel:

PASTED

PHASE 0 — REVERT REPORT Files changed and what was reverted File Change core/dispatch.py:503-519 Replaced single-unit staging ("stage exactly ONE") with original N_needed+1 formula: _n_start = min(max(1, ceil(delta / rated) + 1), len(_offline)), target distributed equally core/simulation_core.py

PASTED

PHASE 1 — STATE/WRITER TABLE Derived from asset_modules.py:962–999 (advance), simulation_core.py:638–644 (_synchronised_units filter), loading.py:116–136 (apply_loading), and asset_modules.py:810–814 (is_synchronised). State Written by advance()? Written by apply_loading() today? Physical meanin

PASTED

All evidence collected. Here is the complete report. PART A — Raw evidence Command 1: sed -n '625,700p' core/simulation_core.py # Used by the arbitrator, the balance decomposition, and the TickResult. _p_dispatch_droop_mw = max(0.0, p_dispatch_required_mw + _droop_correction_mw) #

PASTED

Triage closeout — all items settled Item 1 — TC-CL-1 and TC-CL-2 updated Both tests now assert the new three-branch contract. TC-CL-1b was added as a dedicated named test for the one-tick tolerance (as required by the instruction): Test What it asserts test_tc_cl_1_for_tick_returns_clean_beyon

PASTED