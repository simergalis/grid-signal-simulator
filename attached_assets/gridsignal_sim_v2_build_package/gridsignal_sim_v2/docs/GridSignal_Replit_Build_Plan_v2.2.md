# GridSignal Replit Build Plan v2.2 — Pointer

The authoritative copy of the build plan lives at:

```
gridsignal_sim/docs/GridSignal_Replit_Build_Plan_v2.2.md
```

This file is a pointer only. It exists here so the top-level `docs/` directory
is not empty after de-duplication, but **do not edit this copy** — changes made
here will diverge from the canonical version and be overwritten or ignored.

All other design documents (skeleton audit, design review v0.1/v0.2, remediation
pack, acceptance matrix) are also in `gridsignal_sim/docs/`.
