Triage closeout — settle four items before the refactor

Follows: baseline triage (57 failed / 5 errors → 14 failed / 0 errors). Precedes: the dynamic-configuration refactor (DR-2026-08-06, D-06). Scope: small and bounded. Four corrections, two investigations. Nothing else.

The triage work was sound — the site_config._stored lifespan-contamination diagnosis in particular was difficult and correct. Four items need settling before the next workstream builds on this baseline, because the number and the classification both have to hold up.

Item 1 — test_tc_cl_1 and test_tc_cl_2 are Phase 3 regressions, not stale assertions

Both are currently filed as class D, "asserted old silent-_CLEAN for out-of-range." Both were passing before this session. Phase 3 changed for_tick()'s contract and broke them.

A test that passes before a change and fails after is a regression caused by that change. The no-editing rule in the triage prompt was aimed at assertions inherited from earlier work; it was not licence to change a contract and leave its own tests red.

Update both tests to the new contract, as part of closing Phase 3:

tick_index ∈ [0, len) → normal entry
tick_index == len → _CLEAN, silently (the documented one-tick tolerance)
tick_index > len or negative → RuntimeError

Assert all three branches explicitly. The one-tick tolerance is the branch most likely to be broken later by someone tightening the guard, so it needs its own named test.

Item 2 — Reconcile the overshoot scan

The Phase 3 report states:

"Overshoot scan: zero tests in the current suite trigger the new RuntimeError."

Two tests now fail because of the new RuntimeError. Both statements cannot stand.

The likely reconciliation: test_tc_cl_1/2 call for_tick() directly with an out-of-range index, rather than overshooting during a simulation run. If so the scan result is correct and only its wording is wrong — but say so with evidence, because "no test overshoots" is a claim the next three phases will rely on.

Report which of the two is true, with the call sites.

Item 3 — test_internal_elapsed_unaffected_by_f5 needs evidence for its classification

Filed class D, asserting 40.0 against an actual 115.0. That is a 3× discrepancy on a test whose entire purpose is that fault injection F5 must not affect elapsed-time accounting.

"With ramp_seconds=120" is an explanation only if ramp_seconds genuinely changed from a value that made 40.0 correct. Show the change — the commit, the fixture, or the config default that moved it. Trace what the test asserted when it was written and what moved since.

If no such change exists, this is class C wearing a class D label — an elapsed-time defect sitting directly underneath the timing work the next phases build on. Reclassify and report; do not fix it in this session either way.

Item 4 — Re-scope tasks #217 and #218

test_tc_p0_1/2/3/5 fail because "fixtures use breaker_closed; runtime now reads state/output_mw." Those are precisely the fields the ramp-algorithm work changes again: RAMPING and AT_TARGET are deleted, UNLOADING is added, and synchronised_output_mw becomes on_bus_output_mw (DR-2026-08-06 D-05).

Fixing them now guarantees fixing them twice.

Move test_tc_p0_1/2/3/5 out of #217 and into the ramp-algorithm work. #217 then covers only test_internal_elapsed_unaffected_by_f5 and whatever Item 3 concludes about it. Update both task descriptions.

Investigation 1 — Is site_config._stored meant to be mutable after startup?

The lifespan contamination happened because _stored can be overwritten after process start. Guard A prevents location literals from spreading; it does not prevent the one authorised holder from being reassigned mid-process, which is the failure that actually occurred.

Report:

Every write to site_config._stored, with file:line.
Which of those are legitimate startup configuration, and which are test scaffolding.
Whether any write can occur while a simulation run is live.

Then state whether the setter should raise once a run is active. Do not implement it — this is a design decision for review.

Investigation 2 — Is the pool_recycle teardown failure production-relevant?

NullPool fixes the symptom by removing pooling under pytest. Two consequences worth checking before this is filed closed:

The tests now exercise a connection strategy production does not use. State what coverage that removes.
More importantly: if pool_recycle schedules work on an event loop that can close underneath it, that is plausibly a production shutdown or reload hazard, not solely a pytest artefact. Determine whether the same sequence can occur on app shutdown or a hot reload.

Report findings. Do not change production database configuration in this session.

Item 5 — Restate the baseline

Report the final figure in a form the next phase gate can be measured against:

Pre-existing failures, distinguished from failures introduced by this session's own changes.
Intentional-red count broken out.
Class C and class D lists as they stand after Items 1, 3 and 4.

The honest reading of the current state is 12 pre-existing + 2 owed from Phase 3, not 14 inherited. Confirm or correct that with evidence.

Prohibited
Fixing any class C item.
Fixing test_internal_elapsed_unaffected_by_f5 before its classification is settled.
Fixing test_tc_p0_* — those move to the ramp work.
Modifying any file under core/.
Changing production database configuration.
Implementing the site_config._stored setter guard.
Making for_tick() raise on a one-tick overshoot.
Skipping or xfail-ing anything to lower the count.
Modifying gridsignal_logger.py.
Acceptance criteria
 test_tc_cl_1 and test_tc_cl_2 updated to the new contract; all three branches asserted, including the one-tick tolerance as its own named test.
 Overshoot-scan contradiction reconciled with call-site evidence.
 test_internal_elapsed_unaffected_by_f5 classification settled with evidence for what moved ramp_seconds, or reclassified to C.
 test_tc_p0_1/2/3/5 moved from #217 to the ramp-algorithm task; both task descriptions updated.
 Every write to site_config._stored reported with file:line, and a stated recommendation on post-startup mutation.
 pool_recycle production-hazard question answered yes or no, with evidence.
 Baseline restated with pre-existing and self-inflicted failures separated.
 No class C item fixed; no core/ file modified.
Project content
GridSignal
Created by you
GridSignal-Forecast-Engine-Functional-Spec_v2.5.md

1,683 lines

MD

PDF

PDF

Content
gridsignal_source_review.zip

ZIP

gridsignal_ui_components.zip

ZIP

The gas turbine generators powered up all at once, which is not possible. One turbine shoiuld have started, increasing its oputput, and rthwen another turbine should start up Fix the logic underlying the ramp up 12 minutes ago Refining turbine startup logic Refining turbine startup logi

PASTED

The test_kube_oscillation docstring literally says "With the current hardcode this test FAILS — that is the point" — it's an intentional red test, pre-existing. I need to fix the 3 real regressions: test_d10, test_tc84f, and test_tc_gt2. Let me read their full failures and test code in parallel:

PASTED

PHASE 0 — REVERT REPORT Files changed and what was reverted File Change core/dispatch.py:503-519 Replaced single-unit staging ("stage exactly ONE") with original N_needed+1 formula: _n_start = min(max(1, ceil(delta / rated) + 1), len(_offline)), target distributed equally core/simulation_core.py

PASTED

PHASE 1 — STATE/WRITER TABLE Derived from asset_modules.py:962–999 (advance), simulation_core.py:638–644 (_synchronised_units filter), loading.py:116–136 (apply_loading), and asset_modules.py:810–814 (is_synchronised). State Written by advance()? Written by apply_loading() today? Physical meanin

PASTED

All evidence collected. Here is the complete report. PART A — Raw evidence Command 1: sed -n '625,700p' core/simulation_core.py # Used by the arbitrator, the balance decomposition, and the TickResult. _p_dispatch_droop_mw = max(0.0, p_dispatch_required_mw + _droop_correction_mw) #

PASTED