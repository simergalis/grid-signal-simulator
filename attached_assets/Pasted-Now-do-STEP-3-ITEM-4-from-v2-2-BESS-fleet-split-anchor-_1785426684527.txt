Now do STEP 3 ITEM 4 from v2.2 — BESS fleet split, anchor constraint, reserve aggregation.
This closes Step 3. Stop after it.

Three changes, one commit — they interlock and doing them separately means doing two of them
twice.

1. FLEET SPLIT. DispatchArbitrator.tick() currently hands every BESS unit the same aggregate
shortfall. Split it proportional to each unit's available power and state of charge.

2. ANCHOR CONSTRAINT (v2.5 §7.1.2):
     BESS_bridging_available(t) = min(rated, usable SoC) - P_anchor_reserve
P_anchor_reserve is zero when a unit is grid-following, non-zero when it is the island's
grid-forming anchor. An anchor must retain headroom in BOTH directions to regulate against
disturbance, so its full rating is not available for bridging.

DEPENDENCY YOU WILL HIT: operating mode does not exist in this simulator. Grid-tie and
transition modes are Step 11 (§28). Introduce the minimum needed here — an IslandMode enum on
the site, read each tick, not from static config, because the anchor role changes with mode.
Default to ISLANDED, for two reasons: it is the conservative default TC-63 requires (defaulting
to grid-following makes P_anchor_reserve zero, which silently reproduces the unadjusted
arithmetic), and it is the representative case for this product's market. Leave the mode
transition machinery to Step 11.

3. RESERVE AGGREGATION. dispatch.py currently computes
     total_sustainable_s = min(b.max_sustainable_seconds(peak/n) for b in units)
Named "total", operating min, dividing the peak equally regardless of unit capability. Replace
with: allocate the peak shortfall across units using the SAME split as change 1, then sum each
unit's sustainable duration at ITS OWN allocated share. Note the D11 interaction — per-unit
power ceilings are now enforced, so a unit allocated more than its rating returns 0.0 and the
fleet check must reflect that rather than averaging it away.

The reserve check and the insufficient-reserve alert must both consume bridging_available_mw,
not rated capacity.

Gates: BOTH remaining audit tests must pass —
  test_bess_bridging_excludes_anchor_reserve
  test_anchor_reserve_defaults_conservatively_nonzero
That closes all 13.

Tests: (a) a shortfall exceeding any single unit's rating but within the fleet's combined
capacity is covered — this fails under min(); (b) a heterogeneous fleet where equal division
would misallocate; (c) the anchor unit contributes less bridging than an identical
grid-following unit; (d) demo scenarios still behave — demo-20mw no alert, demo-alert fires.

Expect example_usage.py to move: demo-20mw's 15 MW BESS now has anchor reserve withheld, so
bridging capability drops. If that pushes it into firing an alert, resize rather than reducing
the anchor reserve. Say which you did.

Re-run all four and report, with alerts_seen, 1x AND 2x, and p50.