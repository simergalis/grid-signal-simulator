Turbine Unit Commitment — Revert and Diagnose

Target model: core/simulation_core.py and core/dispatch.py (live engine). NOT gridsignal_logger.py. If any instruction below appears to apply to the logger, stop and report.

This session performs NO fixes. Phase 0 reverts. Phase 1 diagnoses and reports. Do not proceed to any fix, in any file, for any reason. Report and stop.

Phase 0 — Revert

Revert every change made in the previous two sessions, both test-fixture and production.

0.1 Test fixture reverts
Item	Revert to
r_asset_mw_per_s in all touched tests	0.2
dt_lead_seconds	30 (do not apply the proposed change to 5)
Turbine rated_mw in fixtures	15
_N_WARMUP_TICKS	5
_make_schedule_uniform default tick count	20
node_count in test_tc_gt2*	original value (500)
Any assertion edited to match observed behaviour	original assertion
0.2 Production reverts

Both fixes from the earlier session are reverted pending re-design:

stage_for_predicted_step() — restore the original N_start computation. Do not leave N=1.
Headroom check denominator in simulation_core.py — restore state == TurbineState.SYNCHRONISED. Do not leave RAMPING/STARTING counted.
0.3 Report

Output the full diff of the revert. Confirm the test suite failure set now matches the pre-session baseline exactly:

Pre-existing and expected to remain failing: test_turbine_payload_p0 (×4), test_s1_baseline, test_kube_oscillation (intentional red test), fabric scenarios S3/S4/S6/S7/S8 (missing files).
test_d10, test_tc84f, test_tc_gt2, test_power_cap_toggle must return to passing. If any does not, that is a finding — report it and stop.

Stop here. Report before starting Phase 1.

Prohibited for the remainder of this work

The following are spec-derived values, not tuning knobs. Changing any of them to make a test pass is prohibited:

r_asset_mw_per_s (§7.1, MVP default 0.2 MW/s)
dt_lead_seconds (§6.1, §9 — 30–60 s)
Turbine rated_mw
_N_WARMUP_TICKS, schedule length, node_count
Any threshold constant (including the 80% headroom ratio)
Any assertion in an existing test

If a test cannot pass at spec values, that is a finding to report, not a parameter to adjust. Report the finding and stop.

Phase 1 — Diagnostics (report only)

Run all six. Produce a written report with file:line references. Make no code changes.

D1 — Loading-layer override of staged output

The previous session observed: "turbine-0 stages at >10 MW but then the loading layer rapidly drags it down to near-zero."

Report:

Every writer to a turbine's output/target MW, with file:line.
The execution order of those writers within a single tick.
Which writer wins when they disagree, and whether that is by design or by ordering accident.

This is a candidate dual-implementation defect. Do not fix it. Characterise it.

D2 — Minimum stable load and reverse power

Report whether any of the following exist anywhere in the live engine:

Minimum stable load (MSL) constraint
No-load fuel consumption
Reverse power interlock or trip
Minimum run time / minimum down time

For each: present or absent, file:line if present, and the configured value in the demo plant fixture. The Gas Turbine Fleet tile currently displays 0.00 / 0.00 for no-load/MSL on all five units — confirm whether that reflects configuration or a display defect.

State explicitly whether the engine can currently drive a SYNCHRONISED unit to 0.00 MW output without any interlock firing.

D3 — Start-trigger provenance

Report exactly what quantity the next-unit start decision reads, with file:line:

Is it measured output?
Is it forecast ΔP?
Is it a ratio of measured output to rated capacity?

Trace the full expression. Report every input to the decision and where each originates.

D4 — Silent no-op in fault injection

Confirm that _apply_soc_corruption returns an unmodified tick when the tick index exceeds the schedule length, without raising or warning.

Then report: every test in the suite that currently runs past its schedule length, and therefore has fault injection silently disabled for part of its run. List them by name with the tick index at which injection stops.

D5 — BESS re-fire on sequential start (test_d10)

At spec values, produce a tick-by-tick trace of BESS output across a sequential turbine start sequence. Determine which of these is true:

(a) A start command was issued that forecast ΔP did not require, and the BESS is correctly responding to a real shortfall.
(b) The §7.2 step-3 taper is failing the sustained-10-second rule, and the BESS is re-firing when it should be in standby.

Report the evidence for whichever holds. Do not assume it is "expected new behaviour."

D6 — N−1 coverage during ramp-in (test_tc84f)

At spec values, confirm whether COVERED_WITH_SHED during the sequential ramp-in window is a true physical result — that is, whether the surviving fleet genuinely cannot carry demand while units are still coming online.

If true, this is an operational finding about the sequencing policy, not a stale assertion. Report the duration of the exposure window and the maximum shortfall in MW.

Acceptance criteria
 Phase 0 diff reported; failure set matches pre-session baseline exactly.
 No production code modified in Phase 1.
 All six diagnostics reported with file:line references.
 No parameter in the prohibited list changed anywhere.
 No assertion modified anywhere.
 Report ends with "no fixes applied" or an explicit statement of what was changed and why.
Do not
Do not fix anything found in Phase 1.
Do not tune any constant to make any test pass.
Do not edit assertions.
Do not touch gridsignal_logger.py.
Do not proceed past a phase gate without reporting.
Project content
GridSignal
Created by you
GridSignal-Forecast-Engine-Functional-Spec_v2.5.md

1,683 lines

MD

PDF

PDF

Content

The gas turbine generators powered up all at once, which is not possible. One turbine shoiuld have started, increasing its oputput, and rthwen another turbine should start up Fix the logic underlying the ramp up 12 minutes ago Refining turbine startup logic Refining turbine startup logi

PASTED

The test_kube_oscillation docstring literally says "With the current hardcode this test FAILS — that is the point" — it's an intentional red test, pre-existing. I need to fix the 3 real regressions: test_d10, test_tc84f, and test_tc_gt2. Let me read their full failures and test code in parallel:

PASTED