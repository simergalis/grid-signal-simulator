Post the three diagnostic tables and state whether the residual is physical, artifact, or mixed. Do not proceed without confirmation.

Phase 12.1 — Separate flow from meter

Apply only if Phase 12.0 shows the residual is filter-driven.

Split the channel
Channel	Meaning	Filtered?
grid_import_mw	Instantaneous power flow; closes the balance	No
grid_import_metered_mw	Meter reading with realistic response	Yes, τ configurable, default 2 s

balance_residual_mw is computed against the unfiltered flow and means model error only. Meter realism is preserved in the separate channel and may be used for display.

Apply the same review to every other channel that participates in the balance equation: any filter on a conservation term is suspect. Filters belong on measurements, never on the terms that close the balance.

Acceptance criteria
#	Criterion	Threshold
G1	balance_residual_mw sd, steady state, no injected error	< 1% of site load
G2	Residual sd at 0.75 s step period vs 60 s step period	within a factor of 2 (i.e. no longer scales with event duration)
G3	corr(grid_import_mw, −(all other balance terms))	must not exceed 0.9999 unless explicitly tagged as slack
G4	Sum of all balance terms using unfiltered flow	closes to within model tolerance every tick
Phase 12.2 — Islanded slack: frequency, not convenience

Coverage ratio 0.43 is correct behaviour, not a shortfall: the turbine cannot follow a 0.12 s pulse at 0.2 MW/s, and the BESS at τ = 0.3 s covers only part of it. That is the Section 28.1 band boundary appearing in the data.

But the uncovered remainder needs a physically correct destination. In a real islanded microgrid it appears as frequency deviation. Until that exists, it lands in whichever term is most numerically convenient — which is how the residual became ambiguous in the first place.

Implement per Phase 11.3
df/dt = (P_gen - P_load) / (2 * H * S_base) * f_nominal

Defaults: H = 4.0 s, governor droop 4%. Add frequency_hz as a logged channel. In grid-connected mode grid_import_mw remains the slack and must be tagged as such in the schema.

Also verify the BESS time constant

τ = 0.3 s is closer to a slow droop response than to a grid-forming inverter, which responds in low tens of milliseconds. Confirm what the 0.3 s value represents. If it should be ~0.05 s, coverage ratio rises substantially and the residual shrinks without any other change.

Acceptance criteria
#	Criterion	Threshold
I1	Islanded, uncovered step power	appears as frequency_hz deviation, not as unexplained BESS motion
I2	Frequency excursion magnitude vs swing-equation prediction	within ±10%
I3	Islanded, sustained balance	frequency returns to nominal under droop
I4	bess_tau_s documented and traced to an asset class	present in config with a comment
Phase 12.3 — B1 and the setpoint/actual split

Runs after 12.1 and 12.2, since it requires a clean residual.

#	Criterion	Expected
B1	Inject a deliberate 1 MW error into the load model, islanded	frequency deviates measurably; balance_residual_mw non-zero; BESS output does not silently absorb it
B2	BESS output vs bess_setpoint_mw	tracks the setpoint through its modelled time constant; never derived from the residual
B3	bess_setpoint_mw exceeds rated limit	binding_constraint populated and surfaced
B4	Any UI text claiming "BESS standby"	bess_setpoint_mw must be ~0 at that tick

B1 is the whole point of the residual channel. It cannot pass while the residual carries 27 MW of noise, which is why 12.1 comes first.

Phase 12.4 — Forecast trajectory to steady state (task #183)

forecast_mw = sum(g.target_output_mw() for g in state.gpu_modules) with _node_counts populated only via GPUModule.apply_signal() confirms the forecast is queue-derived per Section 4. This is Branch B — a startup ordering bug — not Branch A. IP claim 1 is structurally implemented.

One thing remains ambiguous. The observed 0.13 MW at PUE_base 1.03 implies ~126 kW of rated node power — roughly twelve enterprise_8gpu_air nodes, not "a single node with near-zero weight". And the Forecast Quality chart showed the value climbing from ~0.10 to ~0.38 MW over 15 s, consistent with _node_counts populating progressively.

Capture the full trajectory

Log forecast_mw, nodes_received, and nodes_declared every tick from run start until forecast_mw is stable for 30 s. Then compare:

If forecast_mw converges to nodes_declared × kW_i × PUE_base — the defect is purely the startup race. Fix the ordering in run_manager.py so WorkloadSignal application precedes the first broadcast.
If it settles near 0.38 MW for a 19.96 MW job — there is a separate magnitude bug beneath the race, and fixing the ordering will not touch it.

Do not conclude from the value at first broadcast. The settled value is what distinguishes the two.

Acceptance criteria
#	Criterion	Threshold
F1	Settled forecast_mw vs Section 4 formula on declared allocation	within ±1%
F2	TC-01 conditions (10 nodes, enterprise_8gpu_air 10.2 kW, PUE_base 1.03)	0.1051 MW ± 0.1%
F3	Step change in measured site draw, no WorkloadSignal change	forecast changes by exactly 0.0 MW
F4	Ticks between run start and forecast_mw reaching declared value	0
F5	Header PREDICTED PEAK vs Forecast Quality centre, same tick	bit-identical

F3 is the assertable form of IP claim 1. It now looks likely to pass — assert it explicitly rather than assuming.

Phase 12.5 — TC-03 made deterministic (task #184)

TC-03 currently measures a 2.08% gap against a 2% threshold, with ~1.38% attributable to stochastic COP and pump noise over the averaging window. A test that marginal will pass and fail randomly across CI runs, which trains people to re-run until green.

Two changes:

Noise-disabled mode for this test. TC-03 asserts a deterministic convergence property of the thermal filter. COP and pump noise are contamination, not the subject.
Settle to 7τ rather than 5τ. At 5τ the genuine lag residual is 0.67%, consuming a third of the budget. At 7τ it is 0.09%. The extra sim time is free.
#	Criterion	Threshold
C1	TC-02 — evaluate at t₀ + 60 s	P_cooling(t) = 0
C2	TC-03 — noise disabled, settle 7τ, converge to α_max × P_compute	within 2%, expected ≈ 0.1%
C3	Repeat C2 ten times with different seeds	all pass; spread < 0.5%
C4	0.75 s load oscillation applied	cooling load does not pulse; tracks the mean

C3 exists to prove the flakiness is gone, not merely that one run passed.

Phase 12.6 — Turbine constraints (R4–R6, carried from 11.3b)

Not addressed in Phase 11. Ramp-down now exists and is rate-limited, but the constraints that make ramp-down meaningful do not.

Implement p_min_stable_frac (default 0.45), t_min_run_s (default 1800), t_min_down_s (default 900), and per-unit gt_mode state tracking.

#	Criterion	Expected
R4	Sustained load below p_min_stable, last online unit, islanded	holds at min load; does not ramp below; does not stop
R5	Load below min stable with a second unit online	one unit stops, other holds; t_min_down_s enforced before restart
R6	Repeated checkpoint valleys over 30 min, t_min_run_s active	zero turbine stop/start cycles
R7	Mean ramp rate over any 5 s window vs r_asset_up / r_asset_down	within ±5% (currently 0.104 vs 0.20 — passing)
R8	Header dispatchable MW vs sum of online-unit capability plus BESS	bit-identical, single source

R2 (checkpoint valley suppresses ramp-down) paired with R6 is what makes IP claim 4 demonstrable. Neither is assertable today.

Phase 12.7 — Schema hygiene

Small, cheap, and each one has already cost a round trip in analysis.

bess_power_sign_convention: generator in the CSV header. Convention ambiguity has produced two wrong conclusions in review so far.
Declared units and a channel_source tag per column: measured / derived / modelled / noise_injected.
schema_version, scenario_id, rng_seed on every capture.
Restore compute_load_mw if not already back, and confirm compute_mem_util_pct has non-zero variance.
Keep source: synthetic_generator on export CSVs until consolidation happens.
Deferred, tracked

Export endpoint consolidation. /api/export/telemetry-log should serialize the live engine's TickResult stream rather than running a second physics model. The source label is an acceptable interim, but two implementations will drift, and every future finding will need disambiguating before it can be acted on. Recommend scheduling before the next external demo.

Out of scope
Do not modify Section 6.2 classifier thresholds — band-separation is a spec change, not a build item.
Do not alter dispatch arbitration ordering.
Do not change the hardware profile library.
No LLM, external API, or network dependency.
Deliverables
Phase 12.0 diagnostic tables and residual verdict — before any code change.
Flow/meter split with G1–G4, if indicated by 12.0.
Swing-equation islanded slack, frequency_hz channel, I1–I4.
B1–B4 passing against a clean residual.
Full forecast trajectory capture and F1–F5.
Deterministic TC-03 with C1–C4.
Turbine constraints R4–R8.
Schema hygiene items.
A fresh capture after 12.1, 12.2, and 12.6, each stating which model it came from.
Artifacts
Gridsignal replit prompt stochastic step timing
Document · MD 
Gridsignal replit prompt phase11 forecast path
Document · MD 
Project content
GridSignal
Created by you
GridSignal-Forecast-Engine-Functional-Spec_v2.5.md

1,683 lines

MD

PDF

PDF

Content
system_stats (2).csv

CSV

system_stats (4).csv

CSV

system_stats (10).csv

CSV

Good progress — four of these are real fixes. But one phrase in your report is more consequential than any of the numbers. "A standalone physics simulator" If gridsignal_logger.py sits at workspace root, six levels above export.py, and generates the CSV independently, then you have two physics mod

PASTED

The document says diagnostic only — no fixes, report findings and stop. I need to run A (timescale separation), B (filtered vs unfiltered residual), and C (per-step decomposition). Let me read the logger first to understand the grid_import model before writing the diagnostic. 3 actions Good. I

PASTED