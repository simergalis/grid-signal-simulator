Phase 6 — Peak load: reconcile three definitions, then declare one

Follows: Phase 5 complete. Guards D1, D2, E Tier 1 green. Baseline 12 failed, 965 passed, 974 collected, 0 errors from canonical CWD gridsignal_sim/. Blocks: Phase 7. Scope: Item 1 is a defect that must be settled before the field is added. Items 2–4 follow from it.

DR-2026-08-06 D-04 calls for design_peak_load_mw on ScenarioSpec, replacing the interim observed peak in the fleet modal. That is still the destination. But peak compute — the quantity the new field builds on — is currently computed three different ways, and two of them are in the same function.

Item 1 — _peak_compute_mw has two definitions in one function (blocking)

runtime/scenario_factory.py, inside build_run_context:

python
# line 144 — solar sizing
_peak_compute_mw = node_count * _solar_profile.rated_kw * site.pue_base / 1000.0
_solar_rated_mw  = 0.25 * _peak_compute_mw          # PROTO-7

# line 183 — cooling sizing
_peak_compute_mw = node_count * _solar_profile.rated_kw / 1000.0
_rated_cooling_mw = site.alpha_max * _peak_compute_mw * _COOLING_MARGIN   # PROTO-10

Same name, same scope, rebound. Solar is sized against a PUE-inclusive peak; cooling against a PUE-exclusive one. With pue_base = 1.03 the two differ by 3%, which is small enough to have gone unnoticed and large enough to be wrong.

The comment at line 179 makes it worse, not better: it says the 15% headroom exists partly to cover "PUE-base overhead on total IT load" — so cooling is compensating with a margin for the PUE it excluded from its own basis, while solar includes it directly. One of the two is right; both cannot be.

A third definition sits at line 636, on the spec path:

python
_spec_peak_compute_mw = float(spec_data.get("solar_rated_mw", 0.0)) / 0.25   # reverse PROTO-7

This back-computes peak compute from the solar rating, which was itself derived from peak compute at line 145. A round-trip — the same shape as the 15% margin derivation removed in Phase 5. It inherits the PUE-inclusive definition, then feeds _spec_rated_cooling_mw at line 639, which on the factory path uses the PUE-exclusive one. So the two code paths size cooling from different bases.

Report first, then fix:

Which definition is correct? alpha_max is documented as a fraction of compute — state whether "compute" there means IT load with or without PUE, citing the spec.
Every consumer of each definition, with file:line.
The numeric difference on the demo plant for solar rating, cooling rating, and any downstream figure.

Then collapse to one definition, computed once, with a name that states its basis — _peak_it_load_mw or _peak_compute_draw_mw, not _peak_compute_mw twice. Delete the round-trip at line 636 and derive it from the same source as the factory path.

This changes numbers. Solar or cooling ratings will move on every scenario. Report the delta per scenario and do not adjust any test to accommodate it — a moved number here is a finding.

Gate: one definition, one binding, all consumers reported, numeric deltas stated.

Item 2 — Catalogue the design constants Guard D cannot see

Phase 5 found _COOLING_MARGIN = 1.15 at scenario_factory.py:182. Guard D missed it because it enforces agreement between code and catalogue and says nothing about constants absent from the catalogue — so catalogue completeness is itself unguarded.

Add entries, each with provenance and spec_ref, and read them through core.site_parameters:

Constant	Location	Tag
cooling_margin	scenario_factory.py:182, also inline at :316 and :645	PROTO-10
solar_fraction_of_peak (0.25)	scenario_factory.py:145, :273	PROTO-7 — already marked "CHOSEN, no measured basis"
bess_anchor_reserve_mw (1.0)	storage.ts:109, :117	§7.1.2 design constant

solar_fraction_of_peak appears as a bare 0.25 in the reverse computation at line 636 as well. Once catalogued, all four sites read the same entry.

Provenance must be honest. PROTO-7 already says it has no measured basis; the catalogue entry says CHOSEN.

Item 3 — Define and add design_peak_load_mw

Only after Item 1. ScenarioSpec is at api/schemas.py:337.

Peak load is not peak compute. Site load is compute plus cooling, so the field must state which it means. Recommended: total site load at design point — peak_it_load_mw + rated_cooling_mw — because that is what the N−1 verdict asks about. State the definition in the field docstring; the current PEAK_LOAD_MW = 23.95 corresponded to "compute 19.96 + cooling 3.99", which is the sum, so the sum is the continuity-preserving choice.

Broadcast it on TickPayload, types.ts entry first per task #191.

Every existing scenario needs a value. Derive it at load time from the same single definition rather than hand-entering per scenario, and make the field optional with a documented fallback so old specs still load.

Item 4 — Switch the fleet modal

turbineFleet.ts currently uses peakSiteLoadMW() — the observed peak, labelled "observed peak this run" at lines 592, 659, 770.

Switch n1Covers, n1MarginPct, rampNeedMWs and the READY/ATTENTION verdict to the declared figure, and relabel from "observed this run" to something naming its source. Keep peakSiteLoadMW() in siteParameters.ts — observed peak is still a useful display alongside the design figure, and showing both lets an operator see a site running above its design point.

Prohibited
Adding design_peak_load_mw before Item 1 is settled. Declaring a field on top of three inconsistent bases just fixes one of them in place.
Adjusting any test to accommodate a number moved by Item 1. Report it.
Keeping the round-trip at scenario_factory.py:636.
Adding a catalogue entry without honest provenance. If it has no measured basis, it is CHOSEN.
Making design_peak_load_mw required on ScenarioSpec — old specs must still load.
Deleting peakSiteLoadMW().
Phase 7 work — no settings modal.
Fixing the 3 class C core defects, or test_tc_p0_1/2/3/5.
Modifying gridsignal_logger.py.
Acceptance criteria
 One definition of peak IT load, computed once, named for its basis; PUE inclusion stated and justified against the spec.
 Every consumer of the former three definitions reported with file:line.
 Numeric delta per scenario reported for solar rating, cooling rating, and downstream figures.
 Round-trip at :636 deleted; spec path and factory path share one basis.
 cooling_margin, solar_fraction_of_peak, bess_anchor_reserve_mw catalogued with honest provenance and read through site_parameters.
 design_peak_load_mw on ScenarioSpec with its definition in the docstring; optional with a documented fallback.
 types.ts entry confirmed to precede backend emission.
 Fleet modal N−1 path uses the declared figure; label names its source.
 peakSiteLoadMW() retained.
 Guards D1, D2, E Tier 1 green; TypeScript --noEmit clean.
 Suite reported against 12 / 965 / 974 / 0, any delta attributed.
Project content
GridSignal
Created by you
GridSignal-Forecast-Engine-Functional-Spec_v2.5.md

1,683 lines

MD

PDF

PDF

Content
gridsignal_source_review.zip

ZIP

gridsignal_ui_components.zip

ZIP

The gas turbine generators powered up all at once, which is not possible. One turbine shoiuld have started, increasing its oputput, and rthwen another turbine should start up Fix the logic underlying the ramp up 12 minutes ago Refining turbine startup logic Refining turbine startup logi

PASTED

The test_kube_oscillation docstring literally says "With the current hardcode this test FAILS — that is the point" — it's an intentional red test, pre-existing. I need to fix the 3 real regressions: test_d10, test_tc84f, and test_tc_gt2. Let me read their full failures and test code in parallel:

PASTED

PHASE 0 — REVERT REPORT Files changed and what was reverted File Change core/dispatch.py:503-519 Replaced single-unit staging ("stage exactly ONE") with original N_needed+1 formula: _n_start = min(max(1, ceil(delta / rated) + 1), len(_offline)), target distributed equally core/simulation_core.py

PASTED

PHASE 1 — STATE/WRITER TABLE Derived from asset_modules.py:962–999 (advance), simulation_core.py:638–644 (_synchronised_units filter), loading.py:116–136 (apply_loading), and asset_modules.py:810–814 (is_synchronised). State Written by advance()? Written by apply_loading() today? Physical meanin

PASTED

All evidence collected. Here is the complete report. PART A — Raw evidence Command 1: sed -n '625,700p' core/simulation_core.py # Used by the arbitrator, the balance decomposition, and the TickResult. _p_dispatch_droop_mw = max(0.0, p_dispatch_required_mw + _droop_correction_mw) #

PASTED

Triage closeout — all items settled Item 1 — TC-CL-1 and TC-CL-2 updated Both tests now assert the new three-branch contract. TC-CL-1b was added as a dedicated named test for the one-tick tolerance (as required by the instruction): Test What it asserts test_tc_cl_1_for_tick_returns_clean_beyon

PASTED

Phase 3 closeout — four fixes before Phase 4 Follows: Phase 3 verification (guards green, TypeScript clean, Class B fields correctly reverted). Blocks: Phase 4. Scope: four items. No Phase 4 work, no Tier-2 work. The wire-format audit was the right call and the revert was the right response — find

PASTED

All four items are resolved. Here is the complete closeout report. Item 1 — Line 49 content Current content of that line (it is inside the no-tick early-return branch, so the "45 s" prose never reached it): 49: secondary: undefined, The prose that originally contained the two literal

PASTED

Phase 4 is complete. Here is the full acceptance-criteria report. Phase 4 — Acceptance Criteria Report Ordering gate — types.ts before backend emission Edit File Content 1 frontend/src/types.ts 5 new fields added to TickPayload 2 core/models.py 5 new fields added to TickResult 3a runtime/run

PASTED

Phase 5 is complete. Here is the full acceptance-criteria report. Phase 5 — Acceptance Criteria Report Item 1 — 15% margin derivation (circular) Source confirmed: scenario_factory.py:182 defines _COOLING_MARGIN = 1.15 and uses it three times (lines 184, 316, 645), always tagged PROTO-10-MARGIN.

PASTED