**FIX PROMPT — DEF-001 (silent save failure) and DEF-002 (keyboard access gap)**

Two confirmed defects from the BB-PWR-001–051 execution pass. Fix these two only. Do not touch any other test result, open item, or unrelated code in this pass.

**Diagnostic-before-fix discipline applies:** confirm root cause for each defect before writing any fix. No parameter tuning to make symptoms disappear without understanding why they occurred. Stop-and-report gate after diagnosis, before implementation.

---

## DEF-001 — Error banner does not appear on save failure

**Symptom:** With `PUT /scenarios/{id}` returning 500, `PowerSupplySourcesModal` correctly stays open (does not auto-close) but no red error message renders anywhere in the modal body. Operator has no visual indication the save failed.

**Phase 1 — Diagnose (read-only, no files changed):**
1. Read `PowerSupplySourcesModal.tsx` `handleSave`, the catch block (~line 223), and the error-render JSX (~lines 302–313).
2. Determine why the error state isn't reaching the render, or the render isn't visible. Candidates to check explicitly:
   - Is `error` state actually being set in the catch block, or is the catch swallowing the failure silently (e.g. a fetch rejection type mismatch, or the 500 response not throwing because `.ok` is checked but the body parse throws first)?
   - Is the error-render JSX gated behind a condition that's never true (wrong state variable name, stale closure, conditional rendering bug)?
   - Is the error element rendering but visually hidden/off-screen/zero-height due to a CSS issue?
   - Is there a race where the modal's error state gets reset by another effect before render?
3. Report the confirmed root cause. Do not proceed to Phase 2 until root cause is confirmed, not guessed.

**Phase 2 — Fix (only after Phase 1 report is reviewed and approved):**
1. Fix only the confirmed root cause. No speculative fixes to adjacent code.
2. The fix must not change SAVE/CANCEL behavior on the success path.
3. Add or confirm: error message is visibly styled (red/error color per existing test expectation), stays visible until the operator retries or cancels, and clears on a subsequent successful save or explicit dismissal.

**Phase 3 — Verify:**
1. Re-run BB-PWR-045 exactly as written and confirm PASS.
2. Do not modify BB-PWR-045's wording to fit the fix; the fix must satisfy the existing expected result.

---

## DEF-002 — Aggregate toggle and footer button unreachable via keyboard

**Symptom:** The per-tile aggregate toggle indicators and the "⊙ Select Power" footer button are rendered inside SVG `<foreignObject>` elements and are not reachable via standard Tab traversal. Confirmed via BB-PWR-050 — this is a defect, not a blocked/unverifiable test: the expected keyboard-operable behavior was tested for and did not occur.

**Phase 1 — Diagnose (read-only, no files changed):**
1. Identify every interactive element (aggregate toggle dots, "⊙ Select Power" footer button, and any other clickable control) currently hosted inside an SVG `<foreignObject>` in `PlantDiagram` / `PlantNode`.
2. Confirm whether these elements are real `<button>` elements (per the architecture summary's claim that aggregate toggles "are rendered as `<button>` elements") that are simply excluded from tab order due to `foreignObject` quirks in the browser/renderer, or whether they lack proper interactive semantics entirely (e.g., `<div onClick>` with no `tabindex`, no `role="button"`, no keyboard event handler).
3. Report findings: which specific pattern is causing the gap, and whether it's isolated to these controls or a systemic issue affecting other SVG-hosted interactive elements in the plant diagram (e.g., source tile info icons, other modals opened from tiles).

**Phase 2 — Fix (only after Phase 1 report is reviewed and approved):**
1. Fix must make the affected controls keyboard-focusable (in DOM tab order) and keyboard-activatable (Enter/Space), without changing their visual appearance, position, or mouse-click behavior.
2. If the root cause is a known `foreignObject` tab-order limitation, the fix approach should be reported and approved before implementation — there may be more than one valid approach (e.g., explicit `tabIndex` management, restructuring the interactive layer outside the SVG, or a documented shim). Do not default to the first approach found without flagging alternatives if the fix would meaningfully change component structure.
3. Do not expand scope to a full WCAG audit of the plant diagram in this pass — fix only the elements confirmed unreachable in Phase 1. Broader accessibility work remains SPEC-GAP-PWR-010 (no WCAG level specified) and should be scoped separately.

**Phase 3 — Verify:**
1. Re-run BB-PWR-050 exactly as written and confirm PASS — footer button, pill buttons, SAVE/CANCEL, and aggregate toggle indicators all reachable and operable via Tab + Enter/Space, and Escape still closes the modal.
2. Confirm no regression to mouse-click behavior on the same controls.

---

**Reminders that apply to both:**
- No dual implementations — if a fix requires touching how interactive elements are structured, ensure there's still one code path for click handling shared between mouse and keyboard activation, not two parallel handlers.
- Report suite delta (tests newly passing, any tests newly broken) after Phase 3 for both defects.
- **Stop after Phase 3 verification and report before considering this fix pass complete.** Do not proceed to any other open item (TDD-001, the CANNOT-VERIFY-BLACK-BOX cluster, or remaining BLOCKED tests) without a new prompt.