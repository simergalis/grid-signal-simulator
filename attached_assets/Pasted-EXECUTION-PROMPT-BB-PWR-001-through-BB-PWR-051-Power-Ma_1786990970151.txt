EXECUTION PROMPT — BB-PWR-001 through BB-PWR-051 (Power Management "Select Power")

Scope: Execute the 51 black-box test cases in docs/BB-PWR-test-cases.md against the current running implementation. This is a test-execution and defect-reporting task. Do not fix any defect found during this pass. Fixes are a separate, subsequent prompt gated on review of your findings.

Before starting, read:

docs/BB-PWR-test-cases.md in full, including the Spec Section Audit, Architecture Summary, and Open Items table at the end.
GS-IMPL-PSP-002 §6.1, §3.4/§5 (advisory boundary — inviolable).
acceptance-matrix.md entries for TC-29 and TC-68 (referenced by these tests).

Execution procedure:

Run tests in category order (A → B → C → D), Category C last-in-order items (037–040, 051) may require a completed run per their preconditions — batch accordingly.
For each test, record: PASS / FAIL / BLOCKED (precondition can't be established) / CANNOT-VERIFY-BLACK-BOX (the assertion requires internal state not observable from the UI/API/logs).
For any FAIL, capture: what was expected, what was observed, and the specific screen/log/payload evidence — screenshot or raw log excerpt, not a paraphrase.
For any CANNOT-VERIFY-BLACK-BOX, state what additional surface (e.g., a diagnostics endpoint) would make it verifiable. This applies especially to BB-PWR-026, -027, -031, -040, which carry a NOTE about the signal-absence vs. construction-absence limitation (SPEC-GAP-PWR-011) — confirm whether such a diagnostics surface already exists anywhere in the codebase before marking these CANNOT-VERIFY.
For the three tests flagged as sourced from code comments rather than confirmed behavior (BB-PWR-037, -038, -039 — Solar/Fuel Cell/Grid authority-tier lock states), explicitly check the comment against actual current behavior and note in your report if they've diverged.
Do not modify test case wording, tolerances (±0.05 MW, 0.005 MW threshold), or expected results to make a test pass. If a test's expected result appears wrong given actual (correct) system behavior, flag it as a test-design defect, not a system defect, and report both readings.

Do NOT, under any circumstances, in this pass:

Fix any code, even trivial one-line fixes, even if the fix is obvious.
Adjust the 0.005 MW display threshold or ±0.05 MW tolerance constants.
Add or remove southbound protocol client imports/calls.
Resolve the SPEC-GAP-PWR-### prefix question — that's an open item for the registry owner, not an execution-time decision.
Skip a test because its precondition is inconvenient to set up; mark it BLOCKED with the specific blocker instead.

Report format (stop and report before any further action):

SUMMARY: X/51 PASS, Y FAIL, Z BLOCKED, W CANNOT-VERIFY-BLACK-BOX

FAILURES:
[BB-PWR-###] — expected / observed / evidence

BLOCKED:
[BB-PWR-###] — blocker

CANNOT-VERIFY-BLACK-BOX:
[BB-PWR-###] — what surface would be needed

CODE-COMMENT DIVERGENCE (037/038/039):
[confirm match or report actual observed tier/editable state]

SPEC-GAP-PWR-011 FINDING:
[does a diagnostics/introspection endpoint already exist that would let 026/027/031/040
become true construction-absence tests? yes/no + location if yes]

Stop after producing this report. Do not begin fixing any FAIL until the report is reviewed.