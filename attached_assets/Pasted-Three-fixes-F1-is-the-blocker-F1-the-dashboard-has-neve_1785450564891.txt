Three fixes. F1 is the blocker.

F1 — the dashboard has never rendered data. App.tsx posts {scenario_id, end_sim_time,
playback_speed}; StartRunRequest requires {job_id, node_count}. Every page load 422s.

Fix minimally, without pre-empting Step 8's scenario CRUD: add an optional scenario_preset
field to StartRunRequest accepting "demo-20mw" | "demo-alert" | "demo-5mw" | "demo-baseline",
mapped server-side to the same build_run_context parameters example_usage.py uses — including
bess_rated_mw and bess_usable_mwh, which the API currently cannot express at all. When
scenario_preset is present, job_id/node_count become optional. Update App.tsx to send it.
Step 8 replaces this with real scenario CRUD; this is scaffolding to make Page 1 demonstrable.

F2 — the panel and the alert use different denominators. C1's requirement was that they can
never disagree; they currently do. The panel passes current net_demand_mw, the alert passes
predicted peak shortfall. At t=0 that is "full reserve" beside "Insufficient reserve."

Fix: bess_bridging_seconds must be computed against the BINDING constraint. When a predicted
step is staged and its peak shortfall exceeds current net demand, use the peak shortfall.
Otherwise use current net demand. Add a sibling field naming which — bridging_basis:
"predicted_peak" | "current_demand" | "no_load" — and have AssetReservePanel label it, so the
operator can see the panel is answering the same question the banner asked.
Test: at t=0 in demo-alert, with the alert active, the panel must show 0s and basis
"predicted_peak" — not 86400s.

F3 — minor. dt_lead_next_s reads 40.0s on tick 1, but ramp_seconds is 45.0. The job starts and
advance() runs before the first reading, so the operator never sees the full lead window. Say
whether that is an ordering artifact worth fixing or acceptable; do not change it without
saying which.

Then: start both servers, load demo-alert in a browser, and paste what you actually see for the
alert banner, the bridging readout with its basis label, and the countdown. A screenshot if you
can get one. This is the check I should have asked for before accepting Step 7.